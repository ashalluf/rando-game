shader_type spatial;
render_mode cull_back;

// Solid pieces of the encampment kit (carts, bikes, mattresses, bags). Everything is in
// encampment.gdshaderinc.
#include "res://shaders/encampment.gdshaderinc"
