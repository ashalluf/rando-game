shader_type spatial;
render_mode cull_disabled;

// Single-sided sheets of the encampment kit (tent fly, tarp, cardboard, bedding), seen from both
// sides. Everything is in encampment.gdshaderinc.
#include "res://shaders/encampment.gdshaderinc"
