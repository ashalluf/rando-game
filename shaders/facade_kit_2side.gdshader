shader_type spatial;
render_mode cull_disabled;

// Awning canvas: one sheet seen from above and from the pavement under it. Everything else is
// in facade_kit.gdshaderinc.
#include "res://shaders/facade_kit.gdshaderinc"
